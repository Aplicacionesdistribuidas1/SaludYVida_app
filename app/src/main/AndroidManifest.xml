<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chemapeva.saludyvida">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/lo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/loround"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Actividad_principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".MainActivity1"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ActividadPrincipalCliente" />
        <activity android:name=".EmpleadoActivity" />
        <activity android:name=".Ubicacion_Activity" />
        <activity android:name=".MapsDoctors" />
        <activity android:name=".Chat_Room" />
        <activity android:name=".Chat" />
        <activity android:name=".RecuperacionLo" />
        <activity android:name=".Mostrar_Menu_Activity" />
        <activity
            android:name=".Mapsgym"
            android:label="@string/title_activity_mapsfym" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDeK734uyHQH_5gHm7Yhh2AFH521IdJV20" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity
            android:name=".RecuperacionContra"
            android:label="@string/title_activity_recuperacion_contra"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Recuperacion2" />

        <service android:name=".Mensajes">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity android:name=".Administrador" />
        <activity
            android:name=".ClienteActivity"
            android:label="@string/title_activity_cliente"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CarritoActivity" />
        <activity
            android:name=".activity_seleccion"
            android:label="@string/title_activity_seleccion"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MostrarCarritoActivity" />
        <activity android:name=".Actividad_EditarCliente" />
        <activity android:name=".ConfirmarCompraActivity" />
        <activity android:name=".Editar" />
    </application>

</manifest>